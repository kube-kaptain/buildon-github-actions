# SPDX-License-Identifier: MIT
# Copyright (c) 2025-2026 Kaptain contributors (Fred Cooke)

name: Kubernetes App - Docker Retag
on:
  workflow_call:
    secrets:
      docker-registry-logins-secrets:
        description: 'JSON object of secrets for docker-registry-logins (e.g., {"DOCKER_USER": "x", "DOCKER_PASS": "y"})'
        required: false
    outputs:
      # Version outputs
      version:
        description: 'The generated version'
        value: ${{ jobs.build.outputs.version }}
      version-major:
        description: 'Major version number'
        value: ${{ jobs.build.outputs.version-major }}
      version-minor:
        description: 'Minor version number'
        value: ${{ jobs.build.outputs.version-minor }}
      version-patch:
        description: 'Patch version number'
        value: ${{ jobs.build.outputs.version-patch }}
      version-2-part:
        description: 'Version padded/truncated to 2 parts'
        value: ${{ jobs.build.outputs.version-2-part }}
      version-3-part:
        description: 'Version padded/truncated to 3 parts'
        value: ${{ jobs.build.outputs.version-3-part }}
      version-4-part:
        description: 'Version padded/truncated to 4 parts'
        value: ${{ jobs.build.outputs.version-4-part }}
      docker-tag:
        description: 'Tag for Docker images'
        value: ${{ jobs.build.outputs.docker-tag }}
      docker-image-name:
        description: 'Docker image name'
        value: ${{ jobs.build.outputs.docker-image-name }}
      git-tag:
        description: 'Tag for git'
        value: ${{ jobs.build.outputs.git-tag }}
      project-name:
        description: 'The repository/project name'
        value: ${{ jobs.build.outputs.project-name }}
      is-release:
        description: 'Whether this is a release build'
        value: ${{ jobs.build.outputs.is-release }}
      # Docker retag outputs
      docker-source-image-full-uri:
        description: 'Full source image reference'
        value: ${{ jobs.build.outputs.docker-source-image-full-uri }}
      docker-target-image-full-uri:
        description: 'Full target image reference'
        value: ${{ jobs.build.outputs.docker-target-image-full-uri }}
      docker-image-full-uri:
        description: 'Full docker image reference (alias for docker-target-image-full-uri)'
        value: ${{ jobs.build.outputs.docker-target-image-full-uri }}
      images-pushed:
        description: 'Number of images pushed'
        value: ${{ jobs.build.outputs.images-pushed }}
      # Manifest package outputs
      manifests-zip-sub-path:
        description: 'Directory containing manifests zip file (relative)'
        value: ${{ jobs.build.outputs.manifests-zip-sub-path }}
      manifests-zip-file-name:
        description: 'Name of manifests zip file'
        value: ${{ jobs.build.outputs.manifests-zip-file-name }}
      # Manifest repo provider publish outputs
      manifests-uri:
        description: 'Reference to published manifests (format depends on repo provider)'
        value: ${{ jobs.build.outputs.manifests-uri }}
      manifests-published:
        description: 'Whether manifests were published'
        value: ${{ jobs.build.outputs.manifests-published }}
jobs:
  build:
    name: Build & Publish
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      packages: write
      checks: write
    outputs:
      version: ${{ steps.versions.outputs.version }}
      version-major: ${{ steps.versions.outputs.version-major }}
      version-minor: ${{ steps.versions.outputs.version-minor }}
      version-patch: ${{ steps.versions.outputs.version-patch }}
      version-2-part: ${{ steps.versions.outputs.version-2-part }}
      version-3-part: ${{ steps.versions.outputs.version-3-part }}
      version-4-part: ${{ steps.versions.outputs.version-4-part }}
      docker-tag: ${{ steps.versions.outputs.docker-tag }}
      docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
      git-tag: ${{ steps.versions.outputs.git-tag }}
      project-name: ${{ steps.versions.outputs.project-name }}
      is-release: ${{ steps.versions.outputs.is-release }}
      docker-source-image-full-uri: ${{ steps.docker-build.outputs.docker-source-image-full-uri }}
      docker-target-image-full-uri: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
      images-pushed: ${{ steps.docker-push-all.outputs.images-pushed }}
      manifests-zip-sub-path: ${{ steps.manifest-package.outputs.manifests-zip-sub-path }}
      manifests-zip-file-name: ${{ steps.manifest-package.outputs.manifests-zip-file-name }}
      manifests-uri: ${{ steps.manifest-repo-provider-publish.outputs.manifests-uri }}
      manifests-published: ${{ steps.manifest-repo-provider-publish.outputs.manifests-published }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.1
        with:
          fetch-depth: 0
          fetch-tags: true
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Parse workflow ref
        id: parse
        run: |
          # Extract caller's workflow file path from github.workflow_ref
          # Format: owner/repo/.github/workflows/file.yaml@ref
          workflow_path="${GITHUB_WORKFLOW_REF%%@*}"
          workflow_file=".github/workflows/${workflow_path##*/}"

          echo "Resolving ref for reusable workflow: kubernetes-app-docker-retag.yaml"
          echo "Caller workflow: $workflow_file"

          # Check for local call (dogfooding) - uses: ./.github/workflows/...
          if grep -q 'uses:[[:space:]]*\./.github/workflows/kubernetes-app-docker-retag\.yaml[[:space:]]*$' "$workflow_file" || \
             grep -q 'uses:[[:space:]]*\./.github/workflows/kubernetes-app-docker-retag\.yaml[[:space:]]*#' "$workflow_file"; then
            echo "Local workflow call detected (dogfooding)"
            echo "ref=LOCAL" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Find all uses lines that call this workflow and extract refs
          refs=$(grep -o 'buildon-github-actions/.github/workflows/kubernetes-app-docker-retag\.yaml@[^[:space:]"'"'"']*' "$workflow_file" | sed 's/.*@//')

          if [ -z "$refs" ]; then
            echo "::error::Could not find buildon-github-actions ref in $workflow_file"
            exit 1
          fi

          # Check all refs are identical
          unique_refs=$(echo "$refs" | sort -u)
          ref_count=$(echo "$unique_refs" | wc -l | tr -d ' ')
          echo "Found $ref_count unique ref(s): $(echo "$unique_refs" | tr '\n' ' ')"

          if [ "$ref_count" -gt 1 ]; then
            echo "::error::Multiple different refs found in $workflow_file: $(echo "$unique_refs" | tr '\n' ' ')"
            exit 1
          fi

          echo "Resolved ref: $unique_refs"
          echo "ref=$unique_refs" >> "$GITHUB_OUTPUT"
        env:
          GITHUB_WORKFLOW_REF: ${{ github.workflow_ref }}

      - name: Checkout actions
        if: steps.parse.outputs.ref != 'LOCAL' && success()
        uses: actions/checkout@v5.0.1
        with:
          repository: kube-kaptain/buildon-github-actions
          ref: ${{ steps.parse.outputs.ref }}
          path: .github/buildon-github-actions

      - name: Link local actions
        if: steps.parse.outputs.ref == 'LOCAL' && success()
        run: |
          echo "Linking local actions for dogfooding"
          ln -s "$(pwd)" .github/buildon-github-actions

      # === Validate Tooling ===
      - name: Start check - Validate Tooling
        id: check-validate-tooling
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Validate Tooling'
      - name: Validate tooling
        id: validate-tooling
        uses: ./.github/buildon-github-actions/src/actions/validate-tooling
      - name: Pass check - Validate Tooling
        if: success() && steps.check-validate-tooling.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-validate-tooling.outputs.check-run-id }}
      - name: Fail check - Validate Tooling
        if: failure() && steps.check-validate-tooling.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-validate-tooling.outputs.check-run-id }}

      # === Load Project KaptainPM Docker Logins ===
      - name: Start check - Load Project KaptainPM Docker Logins
        id: check-load-project-kpm
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Load Project KaptainPM Docker Logins'
      - name: Load project KaptainPM docker logins
        id: load-project-kpm
        shell: bash
        env:
          BUILD_PLATFORM: github-actions
          BUILD_PLATFORM_LOG_PROVIDER: github-actions
        run: ./.github/buildon-github-actions/src/scripts/main/load-project-kaptainpm-docker-logins
      - name: Pass check - Load Project KaptainPM Docker Logins
        if: success() && steps.check-load-project-kpm.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-load-project-kpm.outputs.check-run-id }}
      - name: Fail check - Load Project KaptainPM Docker Logins
        if: failure() && steps.check-load-project-kpm.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-load-project-kpm.outputs.check-run-id }}

      # === Pre-Init Docker Registry Logins ===
      - name: Start check - Pre-Init Docker Registry Logins
        id: check-docker-registry-logins-pre-init
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Pre-Init Docker Registry Logins'
      # Pre-init login for layer image pulls during kaptain-init
      # Config comes from project root KaptainPM.yaml via load-project-kpm step
      # GHCR merge handled by the action internally
      - name: Docker registry logins (pre-init)
        id: docker-registry-logins-pre-init
        uses: ./.github/buildon-github-actions/src/actions/docker-registry-logins
        with:
          config: ${{ steps.load-project-kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          secrets-json: ${{ secrets.docker-registry-logins-secrets }}
          output-sub-path: kaptain-out
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
      - name: Pass check - Pre-Init Docker Registry Logins
        if: success() && steps.check-docker-registry-logins-pre-init.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-registry-logins-pre-init.outputs.check-run-id }}
      - name: Fail check - Pre-Init Docker Registry Logins
        if: failure() && steps.check-docker-registry-logins-pre-init.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-registry-logins-pre-init.outputs.check-run-id }}

      # === Kaptain Init ===
      - name: Start check - Kaptain Init
        id: check-kaptain-init
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Kaptain Init'
      # GH-specific defaults for layer resolution (file values win inside the script)
      - name: Prepare registry defaults
        id: registry-defaults
        shell: bash
        run: |
          OWNER="${{ github.repository_owner }}"
          echo "registry=ghcr.io" >> "$GITHUB_OUTPUT"
          echo "namespace=${OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Kaptain init
        id: kaptain-init
        shell: bash
        env:
          BUILD_MODE: build_server
          BUILD_PLATFORM: github-actions
          BUILD_PLATFORM_LOG_PROVIDER: github-actions
          IMAGE_BUILD_COMMAND: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          DOCKER_TARGET_REGISTRY: ${{ steps.registry-defaults.outputs.registry }}
          DOCKER_TARGET_NAMESPACE: ${{ steps.registry-defaults.outputs.namespace }}
        run: ./.github/buildon-github-actions/src/scripts/main/kaptain-init
      - name: Pass check - Kaptain Init
        if: success() && steps.check-kaptain-init.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-kaptain-init.outputs.check-run-id }}
      - name: Fail check - Kaptain Init
        if: failure() && steps.check-kaptain-init.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-kaptain-init.outputs.check-run-id }}

      # === Load Final KaptainPM YAML ===
      - name: Start check - Load Final KaptainPM YAML
        id: check-load-final-kpm
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Load Final KaptainPM YAML'
      - name: Load final KaptainPM YAML
        id: kpm
        shell: bash
        env:
          BUILD_KIND: kubernetes-app-docker-retag
          BUILD_PLATFORM: github-actions
          BUILD_PLATFORM_LOG_PROVIDER: github-actions
        run: ./.github/buildon-github-actions/src/scripts/main/load-final-kaptainpm-yaml
      - name: Pass check - Load Final KaptainPM YAML
        if: success() && steps.check-load-final-kpm.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-load-final-kpm.outputs.check-run-id }}
      - name: Fail check - Load Final KaptainPM YAML
        if: failure() && steps.check-load-final-kpm.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-load-final-kpm.outputs.check-run-id }}

      # === Pre-Build ===
      - name: Start check - Pre-Build
        if: steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH != '' && success()
        id: check-pre-build
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Pre-Build'
      - name: Run pre-build
        if: steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH != '' && success()
        id: pre-build
        uses: ./.github/buildon-github-actions/src/actions/hook-pre-build
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}
      - name: Pass check - Pre-Build
        if: steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH != '' && success() && steps.check-pre-build.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-pre-build.outputs.check-run-id }}
      - name: Fail check - Pre-Build
        if: steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH != '' && failure() && steps.check-pre-build.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-pre-build.outputs.check-run-id }}

      # === Basic Quality Checks ===
      - name: Start check - Basic Quality Checks
        if: github.event_name == 'pull_request' && success()
        id: check-basic-quality
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Basic Quality Checks'
      - name: Basic quality checks
        if: github.event_name == 'pull_request' && success()
        id: quality
        uses: ./.github/buildon-github-actions/src/actions/basic-quality-checks
        with:
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}
          build-kind: kubernetes-app-docker-retag
      - name: Pass check - Basic Quality Checks
        if: github.event_name == 'pull_request' && success() && steps.check-basic-quality.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-basic-quality.outputs.check-run-id }}
      - name: Fail check - Basic Quality Checks
        if: github.event_name == 'pull_request' && failure() && steps.check-basic-quality.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-basic-quality.outputs.check-run-id }}

      # === Resolve Registry ===
      # Resolve target registry and namespace early in workflow
      # Must run before any hooks or docker operations that need these values
      # All downstream steps should reference steps.resolve-registry.outputs.*
      - name: Resolve target registry and namespace
        id: resolve-registry
        uses: ./.github/buildon-github-actions/src/actions/resolve-target-registry-and-namespace
        with:
          docker-target-registry: ${{ steps.kpm.outputs.DOCKER_TARGET_REGISTRY }}
          docker-target-namespace: ${{ steps.kpm.outputs.DOCKER_TARGET_NAMESPACE }}

      # === Docker Registry Logins ===
      - name: Start check - Docker Registry Logins
        id: check-docker-registry-logins
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Docker Registry Logins'
      # Only run if a registry was resolved (skip if not configured)
      - name: Docker registry logins
        id: docker-registry-logins
        if: steps.resolve-registry.outputs.docker-target-registry != '' && success()
        uses: ./.github/buildon-github-actions/src/actions/docker-registry-logins
        with:
          config: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          secrets-json: ${{ secrets.docker-registry-logins-secrets }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
      - name: Pass check - Docker Registry Logins
        if: success() && steps.check-docker-registry-logins.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-registry-logins.outputs.check-run-id }}
      - name: Fail check - Docker Registry Logins
        if: failure() && steps.check-docker-registry-logins.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-registry-logins.outputs.check-run-id }}

      # === Docker Platform Setup ===
      - name: Start check - Docker Platform Setup
        id: check-docker-platform-setup
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Docker Platform Setup'
      - name: Docker platform setup
        id: docker-platform-setup
        uses: ./.github/buildon-github-actions/src/actions/docker-platform-setup
        with:
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
      - name: Pass check - Docker Platform Setup
        if: success() && steps.check-docker-platform-setup.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-platform-setup.outputs.check-run-id }}
      - name: Fail check - Docker Platform Setup
        if: failure() && steps.check-docker-platform-setup.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-platform-setup.outputs.check-run-id }}

      # === Pre-tagging Tests ===
      - name: Start check - Pre-tagging Tests
        if: steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH != '' && success()
        id: check-pre-tagging
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Pre-tagging Tests'
      - name: Run pre-tagging tests
        if: steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH != '' && success()
        id: pre-tagging-tests
        uses: ./.github/buildon-github-actions/src/actions/hook-pre-tagging-tests
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}
      - name: Pass check - Pre-tagging Tests
        if: steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH != '' && success() && steps.check-pre-tagging.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-pre-tagging.outputs.check-run-id }}
      - name: Fail check - Pre-tagging Tests
        if: steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH != '' && failure() && steps.check-pre-tagging.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-pre-tagging.outputs.check-run-id }}

      # === Versions & Naming ===
      - name: Start check - Versions & Naming
        id: check-versions
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Versions & Naming'
      - name: Generate versions and naming
        id: versions
        uses: ./.github/buildon-github-actions/src/actions/versions-and-naming
        with:
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}
          tag-version-use-source-version-exact: ${{ steps.kpm.outputs.TAG_VERSION_USE_SOURCE_VERSION_EXACT }}
      - name: Pass check - Versions & Naming
        if: success() && steps.check-versions.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-versions.outputs.check-run-id }}
      - name: Fail check - Versions & Naming
        if: failure() && steps.check-versions.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-versions.outputs.check-run-id }}

      # === Post-Versions-and-Naming ===
      - name: Start check - Post-Versions-and-Naming
        if: steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH != '' && success()
        id: check-post-versions-and-naming
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Post-Versions-and-Naming'
      - name: Run post-versions-and-naming
        if: steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH != '' && success()
        id: post-versions-and-naming
        uses: ./.github/buildon-github-actions/src/actions/hook-post-versions-and-naming
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}

          # === Step Outputs ===
          # Version outputs (from versions-and-naming)
          version: ${{ steps.versions.outputs.version }}
          version-major: ${{ steps.versions.outputs.version-major }}
          version-minor: ${{ steps.versions.outputs.version-minor }}
          version-patch: ${{ steps.versions.outputs.version-patch }}
          version-2-part: ${{ steps.versions.outputs.version-2-part }}
          version-3-part: ${{ steps.versions.outputs.version-3-part }}
          version-4-part: ${{ steps.versions.outputs.version-4-part }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          git-tag: ${{ steps.versions.outputs.git-tag }}
          project-name: ${{ steps.versions.outputs.project-name }}
          is-release: ${{ steps.versions.outputs.is-release }}
      - name: Pass check - Post-Versions-and-Naming
        if: steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH != '' && success() && steps.check-post-versions-and-naming.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-post-versions-and-naming.outputs.check-run-id }}
      - name: Fail check - Post-Versions-and-Naming
        if: steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH != '' && failure() && steps.check-post-versions-and-naming.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-post-versions-and-naming.outputs.check-run-id }}

      # === Change Source Note ===
      - name: Start check - Change Source Note
        if: steps.versions.outputs.is-release != 'true' && success()
        id: check-change-source-note
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Change Source Note'
      - name: Write change source note
        id: change-source-note-write
        if: steps.versions.outputs.is-release != 'true' && steps.kpm.outputs.CHANGE_SOURCE_NOTE_ENABLED == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/change-source-note-write
      - name: Pass check - Change Source Note
        if: steps.versions.outputs.is-release != 'true' && success() && steps.check-change-source-note.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-change-source-note.outputs.check-run-id }}
      - name: Fail check - Change Source Note
        if: steps.versions.outputs.is-release != 'true' && failure() && steps.check-change-source-note.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-change-source-note.outputs.check-run-id }}

      # === Release Change Data Generate ===
      - name: Start check - Release Change Data Generate
        id: check-release-change-data-generate
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Release Change Data Generate'
      - name: Generate release change data
        id: release-change-data-generate
        uses: ./.github/buildon-github-actions/src/actions/release-change-data-generate
        with:
          version: ${{ steps.versions.outputs.version }}
          project-name: ${{ steps.versions.outputs.project-name }}
          git-tag: ${{ steps.versions.outputs.git-tag }}
          repository-owner: ${{ steps.versions.outputs.repository-owner }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          change-source-note-enabled: ${{ steps.kpm.outputs.CHANGE_SOURCE_NOTE_ENABLED }}
      - name: Pass check - Release Change Data Generate
        if: success() && steps.check-release-change-data-generate.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-release-change-data-generate.outputs.check-run-id }}
      - name: Fail check - Release Change Data Generate
        if: failure() && steps.check-release-change-data-generate.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-release-change-data-generate.outputs.check-run-id }}

      # === Release Change Data OCI Package ===
      - name: Start check - Release Change Data OCI Package
        id: check-release-change-data-oci-package
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Release Change Data OCI Package'
      - name: Package release change data as OCI image
        id: release-change-data-oci-package
        uses: ./.github/buildon-github-actions/src/actions/release-change-data-oci-package
        with:
          version: ${{ steps.versions.outputs.version }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          project-name: ${{ steps.versions.outputs.project-name }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
      - name: Pass check - Release Change Data OCI Package
        if: success() && steps.check-release-change-data-oci-package.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-release-change-data-oci-package.outputs.check-run-id }}
      - name: Fail check - Release Change Data OCI Package
        if: failure() && steps.check-release-change-data-oci-package.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-release-change-data-oci-package.outputs.check-run-id }}

      # === Docker Retag ===
      - name: Start check - Docker Retag
        id: check-docker-retag
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Docker Retag'
      - name: Pull and retag
        id: docker-build
        uses: ./.github/buildon-github-actions/src/actions/docker-build-retag
        with:
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}
          docker-source-registry: ${{ steps.kpm.outputs.DOCKER_SOURCE_REGISTRY }}
          docker-source-namespace: ${{ steps.kpm.outputs.DOCKER_SOURCE_NAMESPACE }}
          docker-source-image-name: ${{ steps.kpm.outputs.DOCKER_SOURCE_IMAGE_NAME }}
          docker-source-tag: ${{ steps.kpm.outputs.DOCKER_SOURCE_TAG }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
      - name: Pass check - Docker Retag
        if: success() && steps.check-docker-retag.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-retag.outputs.check-run-id }}
      - name: Fail check - Docker Retag
        if: failure() && steps.check-docker-retag.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-retag.outputs.check-run-id }}

      # === Generate ConfigMap ===
      - name: Start check - Generate ConfigMap
        if: steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH != '' && success()
        id: check-generate-configmap
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate ConfigMap'
      - name: Generate ConfigMap manifest
        id: generate-configmap
        if: steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH != '' && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-configmap
        with:
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # ConfigMap settings
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          # Global settings
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate ConfigMap
        if: steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH != '' && success() && steps.check-generate-configmap.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-configmap.outputs.check-run-id }}
      - name: Fail check - Generate ConfigMap
        if: steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH != '' && failure() && steps.check-generate-configmap.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-configmap.outputs.check-run-id }}

      # === Generate Secret Template ===
      - name: Start check - Generate Secret Template
        if: steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH != '' && success()
        id: check-generate-secret-template
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate Secret Template'
      - name: Generate Secret template manifest
        id: generate-secret-template
        if: steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH != '' && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-secret-template
        with:
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # Secret template settings
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          # Global settings
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate Secret Template
        if: steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH != '' && success() && steps.check-generate-secret-template.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-secret-template.outputs.check-run-id }}
      - name: Fail check - Generate Secret Template
        if: steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH != '' && failure() && steps.check-generate-secret-template.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-secret-template.outputs.check-run-id }}

      # === Generate ServiceAccount ===
      - name: Start check - Generate ServiceAccount
        if: steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED == 'true' && success()
        id: check-generate-serviceaccount
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate ServiceAccount'
      - name: Generate ServiceAccount manifest
        id: generate-serviceaccount
        if: steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-serviceaccount
        with:
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # ServiceAccount settings
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate ServiceAccount
        if: steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED == 'true' && success() && steps.check-generate-serviceaccount.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-serviceaccount.outputs.check-run-id }}
      - name: Fail check - Generate ServiceAccount
        if: steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED == 'true' && failure() && steps.check-generate-serviceaccount.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-serviceaccount.outputs.check-run-id }}

      # === Generate Workload ===
      - name: Start check - Generate Workload Manifest
        if: steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE != 'none' && success()
        id: check-generate-workload
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate Workload Manifest'
      - name: Generate workload manifest
        id: generate-workload
        if: steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE != 'none' && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-workload
        with:
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # Workload type
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          # Common workload settings
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          kubernetes-workload-min-ready-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_MIN_READY_SECONDS }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          # Container configuration
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          # Security settings
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          # Resource limits
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-resources-ephemeral-storage: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_EPHEMERAL_STORAGE }}
          # Mount paths
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          # Environment variable refs from ConfigMap/Secret keys
          kubernetes-workload-configmap-keys-for-env: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_KEYS_FOR_ENV }}
          kubernetes-workload-secret-keys-for-env: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_KEYS_FOR_ENV }}
          # Lifecycle
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-tolerations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TOLERATIONS }}
          kubernetes-workload-priority-class-name: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRIORITY_CLASS_NAME }}
          # Liveness probe settings
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-liveness-http-scheme: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_SCHEME }}
          kubernetes-workload-probe-liveness-tcp-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_TCP_PORT }}
          kubernetes-workload-probe-liveness-exec-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_EXEC_COMMAND }}
          kubernetes-workload-probe-liveness-grpc-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_GRPC_PORT }}
          kubernetes-workload-probe-liveness-grpc-service: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_GRPC_SERVICE }}
          kubernetes-workload-probe-liveness-initial-delay-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_INITIAL_DELAY_SECONDS }}
          kubernetes-workload-probe-liveness-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_PERIOD_SECONDS }}
          kubernetes-workload-probe-liveness-timeout-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_TIMEOUT_SECONDS }}
          kubernetes-workload-probe-liveness-failure-threshold: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_FAILURE_THRESHOLD }}
          kubernetes-workload-probe-liveness-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_TERMINATION_GRACE_PERIOD_SECONDS }}
          # Readiness probe settings
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-scheme: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_SCHEME }}
          kubernetes-workload-probe-readiness-tcp-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_TCP_PORT }}
          kubernetes-workload-probe-readiness-exec-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_EXEC_COMMAND }}
          kubernetes-workload-probe-readiness-grpc-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_GRPC_PORT }}
          kubernetes-workload-probe-readiness-grpc-service: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_GRPC_SERVICE }}
          kubernetes-workload-probe-readiness-initial-delay-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_INITIAL_DELAY_SECONDS }}
          kubernetes-workload-probe-readiness-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_PERIOD_SECONDS }}
          kubernetes-workload-probe-readiness-timeout-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_TIMEOUT_SECONDS }}
          kubernetes-workload-probe-readiness-failure-threshold: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_FAILURE_THRESHOLD }}
          kubernetes-workload-probe-readiness-success-threshold: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_SUCCESS_THRESHOLD }}
          # Startup probe settings
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-scheme: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_SCHEME }}
          kubernetes-workload-probe-startup-tcp-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_TCP_PORT }}
          kubernetes-workload-probe-startup-exec-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_EXEC_COMMAND }}
          kubernetes-workload-probe-startup-grpc-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_GRPC_PORT }}
          kubernetes-workload-probe-startup-grpc-service: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_GRPC_SERVICE }}
          kubernetes-workload-probe-startup-initial-delay-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_INITIAL_DELAY_SECONDS }}
          kubernetes-workload-probe-startup-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_PERIOD_SECONDS }}
          kubernetes-workload-probe-startup-timeout-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_TIMEOUT_SECONDS }}
          kubernetes-workload-probe-startup-failure-threshold: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_FAILURE_THRESHOLD }}
          kubernetes-workload-probe-startup-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_TERMINATION_GRACE_PERIOD_SECONDS }}
          # StatefulSet-specific settings
          kubernetes-statefulset-service-name: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_SERVICE_NAME }}
          kubernetes-statefulset-pod-management-policy: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_POD_MANAGEMENT_POLICY }}
          kubernetes-statefulset-update-strategy-type: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_UPDATE_STRATEGY_TYPE }}
          kubernetes-statefulset-update-strategy-partition: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_UPDATE_STRATEGY_PARTITION }}
          kubernetes-statefulset-pvc-enabled: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_ENABLED }}
          kubernetes-statefulset-pvc-storage-class: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_STORAGE_CLASS }}
          kubernetes-statefulset-pvc-storage-size: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_STORAGE_SIZE }}
          kubernetes-statefulset-pvc-access-mode: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_ACCESS_MODE }}
          kubernetes-statefulset-pvc-volume-name: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_VOLUME_NAME }}
          kubernetes-statefulset-pvc-mount-path: ${{ steps.kpm.outputs.KUBERNETES_STATEFULSET_PVC_MOUNT_PATH }}
          # DaemonSet-specific settings
          kubernetes-daemonset-update-strategy-type: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_UPDATE_STRATEGY_TYPE }}
          kubernetes-daemonset-max-unavailable: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_MAX_UNAVAILABLE }}
          kubernetes-daemonset-host-network: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_HOST_NETWORK }}
          kubernetes-daemonset-host-pid: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_HOST_PID }}
          kubernetes-daemonset-host-ipc: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_HOST_IPC }}
          kubernetes-daemonset-run-as-non-root: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_RUN_AS_NON_ROOT }}
          kubernetes-daemonset-privileged: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_PRIVILEGED }}
          kubernetes-daemonset-dns-policy: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_DNS_POLICY }}
          kubernetes-daemonset-tolerations: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_TOLERATIONS }}
          kubernetes-daemonset-node-selector: ${{ steps.kpm.outputs.KUBERNETES_DAEMONSET_NODE_SELECTOR }}
          # Global settings
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate Workload Manifest
        if: steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE != 'none' && success() && steps.check-generate-workload.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-workload.outputs.check-run-id }}
      - name: Fail check - Generate Workload Manifest
        if: steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE != 'none' && failure() && steps.check-generate-workload.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-workload.outputs.check-run-id }}

      # === Generate PodDisruptionBudget ===
      - name: Start check - Generate PodDisruptionBudget
        if: steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset') && success()
        id: check-generate-poddisruptionbudget
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate PodDisruptionBudget'
      - name: Generate PodDisruptionBudget manifest
        id: generate-poddisruptionbudget
        if: steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset') && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-poddisruptionbudget
        with:
          # PDB settings
          kubernetes-poddisruptionbudget-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED }}
          kubernetes-poddisruptionbudget-strategy: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_STRATEGY }}
          kubernetes-poddisruptionbudget-value: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_VALUE }}
          kubernetes-poddisruptionbudget-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_NAME_SUFFIX }}
          kubernetes-poddisruptionbudget-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_COMBINED_SUB_PATH }}
          kubernetes-poddisruptionbudget-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_ADDITIONAL_LABELS }}
          kubernetes-poddisruptionbudget-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_ADDITIONAL_ANNOTATIONS }}
          # Workload settings (for smart defaulting)
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # Global settings
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate PodDisruptionBudget
        if: steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset') && success() && steps.check-generate-poddisruptionbudget.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-poddisruptionbudget.outputs.check-run-id }}
      - name: Fail check - Generate PodDisruptionBudget
        if: steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_PODDISRUPTIONBUDGET_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset') && failure() && steps.check-generate-poddisruptionbudget.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-poddisruptionbudget.outputs.check-run-id }}

      # === Generate Service ===
      - name: Start check - Generate Service
        if: steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'daemonset') && success()
        id: check-generate-service
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Generate Service'
      - name: Generate Service manifest
        id: generate-service
        if: steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'daemonset') && success()
        uses: ./.github/buildon-github-actions/src/actions/generate-kubernetes-service
        with:
          # Service settings
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-port-name: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT_NAME }}
          kubernetes-service-node-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NODE_PORT }}
          kubernetes-service-external-name: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_EXTERNAL_NAME }}
          kubernetes-service-external-traffic-policy: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_EXTERNAL_TRAFFIC_POLICY }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}
          # Workload settings (for defaults)
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          # Required context
          project-name: ${{ steps.versions.outputs.project-name }}
          # Global settings
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
      - name: Pass check - Generate Service
        if: steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'daemonset') && success() && steps.check-generate-service.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-generate-service.outputs.check-run-id }}
      - name: Fail check - Generate Service
        if: steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED != 'false' && (steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED == 'true' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'deployment' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'statefulset' || steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE == 'daemonset') && failure() && steps.check-generate-service.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-generate-service.outputs.check-run-id }}

      # === Pre-package Prepare ===
      - name: Start check - Pre-package Prepare
        if: steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH != '' && success()
        id: check-pre-package-prepare
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Pre-package Prepare'
      - name: Run pre-package prepare
        if: steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH != '' && success()
        id: pre-package-prepare
        uses: ./.github/buildon-github-actions/src/actions/hook-pre-package-prepare
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}

          # === Step Outputs ===
          # Version outputs (from versions-and-naming)
          version: ${{ steps.versions.outputs.version }}
          version-major: ${{ steps.versions.outputs.version-major }}
          version-minor: ${{ steps.versions.outputs.version-minor }}
          version-patch: ${{ steps.versions.outputs.version-patch }}
          version-2-part: ${{ steps.versions.outputs.version-2-part }}
          version-3-part: ${{ steps.versions.outputs.version-3-part }}
          version-4-part: ${{ steps.versions.outputs.version-4-part }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          git-tag: ${{ steps.versions.outputs.git-tag }}
          project-name: ${{ steps.versions.outputs.project-name }}
          is-release: ${{ steps.versions.outputs.is-release }}
          # Docker outputs (from docker-build - empty in manifests-only workflows)
          docker-target-image-full-uri: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
          # Manifest outputs (computed path for this position)
          manifests-substituted-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}/manifests/substituted/${{ steps.versions.outputs.project-name }}
      - name: Pass check - Pre-package Prepare
        if: steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH != '' && success() && steps.check-pre-package-prepare.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-pre-package-prepare.outputs.check-run-id }}
      - name: Fail check - Pre-package Prepare
        if: steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH != '' && failure() && steps.check-pre-package-prepare.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-pre-package-prepare.outputs.check-run-id }}

      # === Manifest Package ===
      - name: Start check - Package Manifests
        id: check-manifest-package
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Package Manifests'
      - name: Prepare manifests for packaging
        id: manifest-package-prepare
        uses: ./.github/buildon-github-actions/src/actions/kubernetes-manifests-package-prepare
        with:
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          project-name: ${{ steps.versions.outputs.project-name }}
          version: ${{ steps.versions.outputs.version }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          docker-image-full-uri: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          is-release: ${{ steps.versions.outputs.is-release }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
      - name: Package manifests
        id: manifest-package
        uses: ./.github/buildon-github-actions/src/actions/kubernetes-manifests-package
        with:
          project-name: ${{ steps.versions.outputs.project-name }}
          version: ${{ steps.versions.outputs.version }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
      - name: Pass check - Package Manifests
        if: success() && steps.check-manifest-package.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-manifest-package.outputs.check-run-id }}
      - name: Fail check - Package Manifests
        if: failure() && steps.check-manifest-package.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-manifest-package.outputs.check-run-id }}

      # === Manifest Repo Provider Package ===
      - name: Start check - Package Manifests for Repo Provider
        id: check-manifest-repo-provider-package
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Package Manifests for Repo Provider'
      - name: Package manifests for repo provider
        id: manifest-repo-provider-package
        uses: ./.github/buildon-github-actions/src/actions/kubernetes-manifests-repo-provider-package
        with:
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}
          manifests-zip-sub-path: ${{ steps.manifest-package.outputs.manifests-zip-sub-path }}
          manifests-zip-file-name: ${{ steps.manifest-package.outputs.manifests-zip-file-name }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}-manifests
          version: ${{ steps.versions.outputs.version }}
          project-name: ${{ steps.versions.outputs.project-name }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
      - name: Pass check - Package Manifests for Repo Provider
        if: success() && steps.check-manifest-repo-provider-package.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-manifest-repo-provider-package.outputs.check-run-id }}
      - name: Fail check - Package Manifests for Repo Provider
        if: failure() && steps.check-manifest-repo-provider-package.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-manifest-repo-provider-package.outputs.check-run-id }}

      # === Post-package Tests ===
      - name: Start check - Post-package Tests
        if: steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH != '' && success()
        id: check-post-package
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Post-package Tests'
      - name: Run post-package tests
        if: steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH != '' && success()
        id: post-package-tests
        uses: ./.github/buildon-github-actions/src/actions/hook-post-package-tests
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}

          # === Step Outputs ===
          # Version outputs (from versions-and-naming)
          version: ${{ steps.versions.outputs.version }}
          version-major: ${{ steps.versions.outputs.version-major }}
          version-minor: ${{ steps.versions.outputs.version-minor }}
          version-patch: ${{ steps.versions.outputs.version-patch }}
          version-2-part: ${{ steps.versions.outputs.version-2-part }}
          version-3-part: ${{ steps.versions.outputs.version-3-part }}
          version-4-part: ${{ steps.versions.outputs.version-4-part }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          git-tag: ${{ steps.versions.outputs.git-tag }}
          project-name: ${{ steps.versions.outputs.project-name }}
          is-release: ${{ steps.versions.outputs.is-release }}
          # Docker outputs (from docker-build - empty in manifests-only workflows)
          docker-target-image-full-uri: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
          # Manifest outputs (from manifest-package)
          manifests-substituted-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}/manifests/substituted/${{ steps.versions.outputs.project-name }}
          manifests-zip-sub-path: ${{ steps.manifest-package.outputs.manifests-zip-sub-path }}
          manifests-zip-file-name: ${{ steps.manifest-package.outputs.manifests-zip-file-name }}
          # Manifest repo provider outputs (from manifest-repo-provider-package)
          manifests-uri: ${{ steps.manifest-repo-provider-package.outputs.manifests-uri }}
      - name: Pass check - Post-package Tests
        if: steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH != '' && success() && steps.check-post-package.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-post-package.outputs.check-run-id }}
      - name: Fail check - Post-package Tests
        if: steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH != '' && failure() && steps.check-post-package.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-post-package.outputs.check-run-id }}

      # === Docker Multi-Tag ===
      - name: Start check - Docker Multi-Tag
        if: steps.kpm.outputs.DOCKER_PUSH_TARGETS != '' && steps.versions.outputs.is-release == 'true' && success()
        id: check-docker-multi-tag
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Docker Multi-Tag'
      - name: Tag images for additional registries
        id: docker-multi-tag
        if: steps.kpm.outputs.DOCKER_PUSH_TARGETS != '' && steps.versions.outputs.is-release == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/docker-multi-tag
        with:
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
      - name: Pass check - Docker Multi-Tag
        if: steps.kpm.outputs.DOCKER_PUSH_TARGETS != '' && steps.versions.outputs.is-release == 'true' && success() && steps.check-docker-multi-tag.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-multi-tag.outputs.check-run-id }}
      - name: Fail check - Docker Multi-Tag
        if: steps.kpm.outputs.DOCKER_PUSH_TARGETS != '' && steps.versions.outputs.is-release == 'true' && failure() && steps.check-docker-multi-tag.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-multi-tag.outputs.check-run-id }}

      # === GitHub Release Prepare ===
      - name: Start check - GitHub Release Prepare
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && success()
        id: check-github-release-prepare
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → GitHub Release Prepare'
      - name: Resolve internal release files
        id: resolve-internal-release-files
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && success()
        shell: bash
        run: |
          echo "verbatim-files=" >> "${GITHUB_OUTPUT}"
          echo "substituted-files=" >> "${GITHUB_OUTPUT}"
          echo "raw-files=" >> "${GITHUB_OUTPUT}"

      - name: Prepare release files
        id: github-release-prepare
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/github-release-prepare
        with:
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-raw-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_RAW_FILES }}
          internal-verbatim-files: >-
            ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}/release-change-data/release-change-data.yaml,
            ${{ steps.resolve-internal-release-files.outputs.verbatim-files }}
          internal-substituted-files: ${{ steps.resolve-internal-release-files.outputs.substituted-files }}
          internal-raw-files: >-
            ${{ steps.manifest-package.outputs.manifests-zip-sub-path }}/${{ steps.manifest-package.outputs.manifests-zip-file-name }},
            ${{ steps.resolve-internal-release-files.outputs.raw-files }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          version: ${{ steps.versions.outputs.version }}
          project-name: ${{ steps.versions.outputs.project-name }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          is-release: ${{ steps.versions.outputs.is-release }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}
          docker-image-full-uri: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          manifests-zip-file-name: ${{ steps.manifest-package.outputs.manifests-zip-file-name }}
      - name: Pass check - GitHub Release Prepare
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && success() && steps.check-github-release-prepare.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-github-release-prepare.outputs.check-run-id }}
      - name: Fail check - GitHub Release Prepare
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && failure() && steps.check-github-release-prepare.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-github-release-prepare.outputs.check-run-id }}

      # === Git Tag Push ===
      - name: Start check - Git Tag Push
        if: steps.versions.outputs.is-release == 'true' && success()
        id: check-git-push-tag
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Git Tag Push'
      - name: Push git tag
        if: steps.versions.outputs.is-release == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/git-push-tag
        with:
          git-tag: ${{ steps.versions.outputs.git-tag }}
          is-release: ${{ steps.versions.outputs.is-release }}
      - name: Pass check - Git Tag Push
        if: steps.versions.outputs.is-release == 'true' && success() && steps.check-git-push-tag.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-git-push-tag.outputs.check-run-id }}
      - name: Fail check - Git Tag Push
        if: steps.versions.outputs.is-release == 'true' && failure() && steps.check-git-push-tag.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-git-push-tag.outputs.check-run-id }}

      # === Manifest Repo Provider Publish ===
      - name: Start check - Publish Manifests via Repo Provider
        if: steps.versions.outputs.is-release == 'true' && success()
        id: check-manifest-repo-provider-publish
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Publish Manifests via Repo Provider'
      - name: Publish manifests via repo provider
        id: manifest-repo-provider-publish
        if: steps.versions.outputs.is-release == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/kubernetes-manifests-repo-provider-publish
        with:
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-uri: ${{ steps.manifest-repo-provider-package.outputs.manifests-uri }}
          manifests-zip-sub-path: ${{ steps.manifest-repo-provider-package.outputs.manifests-zip-sub-path }}
          manifests-zip-file-name: ${{ steps.manifest-repo-provider-package.outputs.manifests-zip-file-name }}
          version: ${{ steps.manifest-repo-provider-package.outputs.version }}
          is-release: ${{ steps.versions.outputs.is-release }}
          repo-provider-url: ${{ steps.manifest-repo-provider-package.outputs.repo-provider-url }}
          repo-provider-namespace: ${{ steps.manifest-repo-provider-package.outputs.repo-provider-namespace }}
          repo-provider-name: ${{ steps.manifest-repo-provider-package.outputs.repo-provider-name }}
          repo-provider-version: ${{ steps.manifest-repo-provider-package.outputs.repo-provider-version }}
      - name: Pass check - Publish Manifests via Repo Provider
        if: steps.versions.outputs.is-release == 'true' && success() && steps.check-manifest-repo-provider-publish.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-manifest-repo-provider-publish.outputs.check-run-id }}
      - name: Fail check - Publish Manifests via Repo Provider
        if: steps.versions.outputs.is-release == 'true' && failure() && steps.check-manifest-repo-provider-publish.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-manifest-repo-provider-publish.outputs.check-run-id }}

      # === Docker Push All ===
      - name: Start check - Docker Push All
        if: steps.versions.outputs.is-release == 'true' && success()
        id: check-docker-push-all
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Docker Push All'
      - name: Push all images
        id: docker-push-all
        if: steps.versions.outputs.is-release == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/docker-push-all
        with:
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}
          is-release: ${{ steps.versions.outputs.is-release }}
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
      - name: Pass check - Docker Push All
        if: steps.versions.outputs.is-release == 'true' && success() && steps.check-docker-push-all.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-docker-push-all.outputs.check-run-id }}
      - name: Fail check - Docker Push All
        if: steps.versions.outputs.is-release == 'true' && failure() && steps.check-docker-push-all.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-docker-push-all.outputs.check-run-id }}

      # === GitHub Release ===
      - name: Start check - GitHub Release
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && steps.versions.outputs.is-release == 'true' && success()
        id: check-github-release
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → GitHub Release'
      - name: Compute release notes
        id: release-notes
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && steps.versions.outputs.is-release == 'true' && success()
        shell: bash
        env:
          USER_NOTES: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          USER_NOTES_FILE: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          VERSION: ${{ steps.versions.outputs.version }}
          PROJECT_NAME: ${{ steps.versions.outputs.project-name }}
          DOCKER_URI: ${{ steps.docker-build.outputs.docker-target-image-full-uri }}
          MANIFESTS_URI: ${{ steps.manifest-repo-provider-publish.outputs.manifests-uri }}
          MANIFESTS_ZIP_FILE_NAME: ${{ steps.manifest-package.outputs.manifests-zip-file-name }}
          RELEASE_CHANGE_DATA_URI: ${{ steps.release-change-data-oci-package.outputs.release-change-data-image-uri }}
          OUTPUT_SUB_PATH: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          BUILD_PLATFORM: github-actions
          BUILD_PLATFORM_LOG_PROVIDER: github-actions
        run: |
          display_name=$(./.github/buildon-github-actions/src/scripts/util/format-project-title "${PROJECT_NAME}")
          echo "title=${VERSION} — ${display_name}" >> "${GITHUB_OUTPUT}"

          if [[ -n "${USER_NOTES_FILE}" ]]; then
            # Notes come from file, skip inline notes computation
            echo "notes=" >> "${GITHUB_OUTPUT}"
          elif [[ -n "${USER_NOTES}" ]]; then
            # User provided notes override entirely
            {
              echo "notes<<NOTES_EOF"
              echo "${USER_NOTES}"
              echo "NOTES_EOF"
            } >> "${GITHUB_OUTPUT}"
          else
            # Default format
            release_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/download/${VERSION}"
            {
              echo "notes<<NOTES_EOF"
              echo "Automatic Kube App release for version ${VERSION} of ${PROJECT_NAME} by Kaptain build script. This release page has the manifest zip attached as well as the main docker image, and the manifests docker image with the same zip inside for automatic consumption."
              echo ""
              echo "### Release Assets"
              echo "* Kube Manifests: [${MANIFESTS_ZIP_FILE_NAME}](${release_url}/${MANIFESTS_ZIP_FILE_NAME})"
              echo "* Docker Image: [${DOCKER_URI}](https://${DOCKER_URI})"
              echo "* Manifests Image: [${MANIFESTS_URI}](https://${MANIFESTS_URI})"
              echo "* Release Change Data: [release-change-data-${VERSION}.yaml](${release_url}/release-change-data-${VERSION}.yaml)"
              echo "* Release Change Data Image: [${RELEASE_CHANGE_DATA_URI}](https://${RELEASE_CHANGE_DATA_URI})"
              ./.github/buildon-github-actions/src/scripts/util/format-release-image-list "${OUTPUT_SUB_PATH}" "${VERSION}"
              echo "NOTES_EOF"
            } >> "${GITHUB_OUTPUT}"
          fi

      - name: GitHub Release
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && steps.versions.outputs.is-release == 'true' && success()
        uses: ./.github/buildon-github-actions/src/actions/github-release
        with:
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          github-release-tag: ${{ steps.versions.outputs.version }}
          github-release-notes: ${{ steps.release-notes.outputs.notes }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-title: ${{ steps.release-notes.outputs.title }}
      - name: Pass check - GitHub Release
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && steps.versions.outputs.is-release == 'true' && success() && steps.check-github-release.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-github-release.outputs.check-run-id }}
      - name: Fail check - GitHub Release
        if: steps.kpm.outputs.GITHUB_RELEASE_ENABLED == 'true' && steps.versions.outputs.is-release == 'true' && failure() && steps.check-github-release.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-github-release.outputs.check-run-id }}

      # === Post-Build ===
      - name: Start check - Post-Build
        if: steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH != '' && success()
        id: check-post-build
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: start
          check-name: 'Build & Publish → Post-Build'
      - name: Run post-build
        if: steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH != '' && success()
        id: post-build
        uses: ./.github/buildon-github-actions/src/actions/hook-post-build
        with:
          image-build-command: ${{ steps.validate-tooling.outputs.IMAGE_BUILD_COMMAND }}
          docker-platform: ${{ steps.kpm.outputs.DOCKER_PLATFORM }}
          # === Workflow Inputs ===
          # Build output
          output-sub-path: ${{ steps.kpm.outputs.OUTPUT_SUB_PATH }}
          docker-push-image-list-file: ${{ steps.docker-registry-logins.outputs.docker-push-image-list-file }}

          # Docker registry logins
          docker-registry-logins: ${{ steps.kpm.outputs.DOCKER_REGISTRY_LOGINS }}
          # Docker build options
          dockerfile-sub-path: ${{ steps.kpm.outputs.DOCKERFILE_SUB_PATH }}
          dockerfile-squash: ${{ steps.kpm.outputs.DOCKERFILE_SQUASH }}
          dockerfile-no-cache: ${{ steps.kpm.outputs.DOCKERFILE_NO_CACHE }}
          # Docker target config (from early resolver)
          docker-target-registry: ${{ steps.resolve-registry.outputs.docker-target-registry }}
          docker-target-namespace: ${{ steps.resolve-registry.outputs.docker-target-namespace }}
          # Docker push targets
          docker-push-targets: ${{ steps.kpm.outputs.DOCKER_PUSH_TARGETS }}

          # Manifests source
          manifests-sub-path: ${{ steps.kpm.outputs.MANIFESTS_SUB_PATH }}
          # Manifests packaging
          manifests-repo-provider-type: ${{ steps.kpm.outputs.MANIFESTS_REPO_PROVIDER_TYPE }}
          manifests-packaging-base-image: ${{ steps.kpm.outputs.MANIFESTS_PACKAGING_BASE_IMAGE }}

          # Kubernetes Generation
          kubernetes-global-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_LABELS }}
          kubernetes-global-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_GLOBAL_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ConfigMap Generation
          kubernetes-configmap-sub-path: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_SUB_PATH }}
          kubernetes-configmap-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_NAME_CHECKSUM_INJECTION }}
          kubernetes-configmap-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_LABELS }}
          kubernetes-configmap-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_CONFIGMAP_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Secret template generation
          kubernetes-secret-template-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_SUB_PATH }}
          kubernetes-secret-template-name-checksum-injection: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_NAME_CHECKSUM_INJECTION }}
          kubernetes-secret-template-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_LABELS }}
          kubernetes-secret-template-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SECRET_TEMPLATE_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes ServiceAccount generation
          kubernetes-serviceaccount-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_GENERATION_ENABLED }}
          kubernetes-serviceaccount-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_NAME_SUFFIX }}
          kubernetes-serviceaccount-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_COMBINED_SUB_PATH }}
          kubernetes-serviceaccount-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_LABELS }}
          kubernetes-serviceaccount-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICEACCOUNT_ADDITIONAL_ANNOTATIONS }}
          # Kubernetes Workload generation
          kubernetes-workload-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TYPE }}
          kubernetes-workload-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_NAME_SUFFIX }}
          kubernetes-workload-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_COMBINED_SUB_PATH }}
          kubernetes-workload-env-sub-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ENV_SUB_PATH }}
          kubernetes-workload-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_LABELS }}
          kubernetes-workload-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_ADDITIONAL_ANNOTATIONS }}
          kubernetes-workload-container-port: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONTAINER_PORT }}
          kubernetes-workload-image-reference-style: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_REFERENCE_STYLE }}
          kubernetes-workload-readonly-root-filesystem: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_READONLY_ROOT_FILESYSTEM }}
          kubernetes-workload-seccomp-profile: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECCOMP_PROFILE }}
          kubernetes-workload-automount-service-account-token: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AUTOMOUNT_SERVICE_ACCOUNT_TOKEN }}
          kubernetes-workload-image-pull-secrets: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_IMAGE_PULL_SECRETS }}
          kubernetes-workload-resources-memory: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_MEMORY }}
          kubernetes-workload-resources-cpu-request: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_REQUEST }}
          kubernetes-workload-resources-cpu-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_RESOURCES_CPU_LIMIT }}
          kubernetes-workload-configmap-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_CONFIGMAP_MOUNT_PATH }}
          kubernetes-workload-secret-mount-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_SECRET_MOUNT_PATH }}
          kubernetes-workload-termination-grace-period-seconds: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_TERMINATION_GRACE_PERIOD_SECONDS }}
          kubernetes-workload-prestop-command: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PRESTOP_COMMAND }}
          kubernetes-workload-affinity-strategy: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_AFFINITY_STRATEGY }}
          kubernetes-workload-probe-liveness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_CHECK_TYPE }}
          kubernetes-workload-probe-readiness-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_CHECK_TYPE }}
          kubernetes-workload-probe-startup-check-type: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_CHECK_TYPE }}
          kubernetes-workload-probe-liveness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_LIVENESS_HTTP_PATH }}
          kubernetes-workload-probe-readiness-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_READINESS_HTTP_PATH }}
          kubernetes-workload-probe-startup-http-path: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_PROBE_STARTUP_HTTP_PATH }}
          kubernetes-workload-replicas: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REPLICAS }}
          kubernetes-workload-revision-history-limit: ${{ steps.kpm.outputs.KUBERNETES_WORKLOAD_REVISION_HISTORY_LIMIT }}
          # Kubernetes Service generation
          kubernetes-service-generation-enabled: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_GENERATION_ENABLED }}
          kubernetes-service-type: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TYPE }}
          kubernetes-service-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PORT }}
          kubernetes-service-target-port: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_TARGET_PORT }}
          kubernetes-service-protocol: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_PROTOCOL }}
          kubernetes-service-name-suffix: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_NAME_SUFFIX }}
          kubernetes-service-combined-sub-path: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_COMBINED_SUB_PATH }}
          kubernetes-service-additional-labels: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_LABELS }}
          kubernetes-service-additional-annotations: ${{ steps.kpm.outputs.KUBERNETES_SERVICE_ADDITIONAL_ANNOTATIONS }}

          # Token substitution
          token-delimiter-style: ${{ steps.kpm.outputs.TOKEN_DELIMITER_STYLE }}
          token-name-style: ${{ steps.kpm.outputs.TOKEN_NAME_STYLE }}
          token-name-validation: ${{ steps.kpm.outputs.TOKEN_NAME_VALIDATION }}
          allow-builtin-token-override: ${{ steps.kpm.outputs.ALLOW_BUILTIN_TOKEN_OVERRIDE }}
          config-sub-path: ${{ steps.kpm.outputs.CONFIG_SUB_PATH }}
          config-value-trailing-newline: ${{ steps.kpm.outputs.CONFIG_VALUE_TRAILING_NEWLINE }}
          token-substitution-passes: ${{ steps.kpm.outputs.TOKEN_SUBSTITUTION_PASSES }}

          # Naming, Versions, and Tags calculation
          release-branch: ${{ steps.kpm.outputs.RELEASE_BRANCH }}
          additional-release-branches: ${{ steps.kpm.outputs.ADDITIONAL_RELEASE_BRANCHES }}
          tag-version-max-parts: ${{ steps.kpm.outputs.TAG_VERSION_MAX_PARTS }}
          tag-version-calculation-strategy: ${{ steps.kpm.outputs.TAG_VERSION_CALCULATION_STRATEGY }}
          tag-version-pattern-type: ${{ steps.kpm.outputs.TAG_VERSION_PATTERN_TYPE }}
          tag-version-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_PREFIX_PARTS }}
          tag-version-source-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_SUB_PATH }}
          tag-version-source-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_FILE_NAME }}
          tag-version-source-custom-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_CUSTOM_PATTERN }}
          tag-version-source-two-sub-path: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_SUB_PATH }}
          tag-version-source-two-file-name: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_FILE_NAME }}
          tag-version-source-two-pattern: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PATTERN }}
          tag-version-source-two-prefix-parts: ${{ steps.kpm.outputs.TAG_VERSION_SOURCE_TWO_PREFIX_PARTS }}

          # Quality check options
          block-slashes: ${{ steps.kpm.outputs.BLOCK_SLASHES }}
          qc-block-slash-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_SLASH_CONTAINING_BRANCHES }}
          qc-block-double-hyphen-containing-branches: ${{ steps.kpm.outputs.QC_BLOCK_DOUBLE_HYPHEN_CONTAINING_BRANCHES }}
          qc-require-conventional-branches: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_BRANCHES }}
          qc-require-conventional-commits: ${{ steps.kpm.outputs.QC_REQUIRE_CONVENTIONAL_COMMITS }}
          qc-block-conventional-commits: ${{ steps.kpm.outputs.QC_BLOCK_CONVENTIONAL_COMMITS }}

          # Hook script paths
          hook-pre-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_BUILD_SCRIPT_SUB_PATH }}
          hook-pre-tagging-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_TAGGING_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-docker-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_DOCKER_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-docker-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_DOCKER_TESTS_SCRIPT_SUB_PATH }}
          hook-pre-package-prepare-script-sub-path: ${{ steps.kpm.outputs.HOOK_PRE_PACKAGE_PREPARE_SCRIPT_SUB_PATH }}
          hook-post-package-tests-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_PACKAGE_TESTS_SCRIPT_SUB_PATH }}
          hook-post-versions-and-naming-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_VERSIONS_AND_NAMING_SCRIPT_SUB_PATH }}
          hook-post-build-script-sub-path: ${{ steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH }}

          # GitHub release
          github-release-enabled: ${{ steps.kpm.outputs.GITHUB_RELEASE_ENABLED }}
          github-release-substituted-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_SUBSTITUTED_FILES }}
          github-release-verbatim-files: ${{ steps.kpm.outputs.GITHUB_RELEASE_VERBATIM_FILES }}
          github-release-notes: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES }}
          github-release-notes-file: ${{ steps.kpm.outputs.GITHUB_RELEASE_NOTES_FILE }}
          github-release-add-version-to-filenames: ${{ steps.kpm.outputs.GITHUB_RELEASE_ADD_VERSION_TO_FILENAMES }}

          # === Step Outputs ===
          # Version outputs (from versions-and-naming)
          version: ${{ steps.versions.outputs.version }}
          version-major: ${{ steps.versions.outputs.version-major }}
          version-minor: ${{ steps.versions.outputs.version-minor }}
          version-patch: ${{ steps.versions.outputs.version-patch }}
          version-2-part: ${{ steps.versions.outputs.version-2-part }}
          version-3-part: ${{ steps.versions.outputs.version-3-part }}
          version-4-part: ${{ steps.versions.outputs.version-4-part }}
          docker-tag: ${{ steps.versions.outputs.docker-tag }}
          docker-image-name: ${{ steps.versions.outputs.docker-image-name }}
          git-tag: ${{ steps.versions.outputs.git-tag }}
          project-name: ${{ steps.versions.outputs.project-name }}
          is-release: ${{ steps.versions.outputs.is-release }}
      - name: Pass check - Post-Build
        if: steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH != '' && success() && steps.check-post-build.outputs.check-run-id != ''
        continue-on-error: true
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: pass
          check-run-id: ${{ steps.check-post-build.outputs.check-run-id }}
      - name: Fail check - Post-Build
        if: steps.kpm.outputs.HOOK_POST_BUILD_SCRIPT_SUB_PATH != '' && failure() && steps.check-post-build.outputs.check-run-id != ''
        uses: ./.github/buildon-github-actions/src/actions/github-check-run
        with:
          action: fail
          check-run-id: ${{ steps.check-post-build.outputs.check-run-id }}
