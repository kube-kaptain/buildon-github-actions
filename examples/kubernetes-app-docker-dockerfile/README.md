# Kubernetes App - Docker Dockerfile

> See [Guide Usage](../guides/README.md) for how to use the three files.

Minimal build with no overrides.

Full Kubernetes application pipeline. Builds a Docker image from a
Dockerfile, generates Kubernetes manifests, and packages everything as OCI
artifacts. The complete build-to-deploy pipeline.
