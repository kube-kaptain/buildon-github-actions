# SPDX-License-Identifier: CC0-1.0
# This example is released to the public domain. Use freely without attribution.

apiVersion: kaptain.org/1.16
kind: docker-build-dockerfile

spec:
  main:
    docker:
      targetRegistry: harbor.internal.example.com
      targetNamespace: platform-team
      logins:
        harbor.internal.example.com:
          type: username-password
          usernameSecret: HARBOR_USER
          passwordSecret: HARBOR_PASS
