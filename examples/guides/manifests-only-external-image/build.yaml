# SPDX-License-Identifier: CC0-1.0
# This example is released to the public domain. Use freely without attribution.

name: Manifests Only - External Image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: kube-kaptain/buildon-github-actions/.github/workflows/kubernetes-app-manifests-only.yaml@1.1.2
    permissions:
      contents: write
      packages: write
      checks: write
