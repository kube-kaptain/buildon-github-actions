# Spec Check Filter Release

> See [Guide Usage](../guides/README.md) for how to use the three files.

Minimal build with no overrides.

Validates and packages spec/schema files as OCI artifacts. Used for
publishing versioned specifications that other projects consume as layers.
